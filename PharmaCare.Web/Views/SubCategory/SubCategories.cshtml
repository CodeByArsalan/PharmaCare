@using PharmaCare.Domain.Models.Configuration
@using PharmaCare.Domain.ViewModels
@using PharmaCare.Web.Utilities
@model SubCategoriesViewModel

@{
    ViewData["Title"] = "Sub Categories";
}

<!-- Add/Edit SubCategory Card -->
<div class="card card-outline rounded-0 card-navy mb-4">
    <div class="card-header">
        <h5 class="card-title mb-0">
            <i class='bx @(Model.IsEditMode ? "bx-edit" : "bx-plus-circle") me-1'></i>
            @(Model.IsEditMode ? "Edit Sub Category" : "Add Sub Category")
        </h5>
    </div>
    <div class="card-body mt-2">
        <form asp-action="@(Model.IsEditMode ? "EditSubCategory" : "AddSubCategory")" method="post">
            @Html.AntiForgeryToken()
            <input type="hidden" asp-for="CurrentSubCategory.SubCategoryID" />
            <input type="hidden" asp-for="IsEditMode" />

            <div asp-validation-summary="ModelOnly" class="alert alert-danger"></div>

            <!-- Label Row -->
            <div class="row">
                <div class="col-md-5">
                    <label asp-for="CurrentSubCategory.Category_ID" class="form-label">Category <span
                            class="text-danger">*</span></label>
                </div>
                <div class="col-md-7">
                    <label asp-for="CurrentSubCategory.SubCategoryName" class="form-label">Sub Category Name <span
                            class="text-danger">*</span></label>
                </div>
            </div>

            <!-- Input and Button Row -->
            <div class="row">
                <div class="col-md-5 col-lg-5">
                    <select asp-for="CurrentSubCategory.Category_ID" asp-items="Model.Categories"
                        class="form-select select2">
                        <option value="">-- Select Category --</option>
                    </select>
                </div>
                <div class="col-md-5 col-lg-5">
                    <input asp-for="CurrentSubCategory.SubCategoryName" class="form-control"
                        placeholder="Enter Sub Category Name" />
                </div>
                <div class="col-md-2 col-lg-2">
                    <div class="d-flex gap-2">
                        <button type="submit" class="btn @(Model.IsEditMode ? "btn-info" : "btn-success")">
                            <i class='bx @(Model.IsEditMode ? "bx-check" : "bx-plus") me-1'></i>
                            @(Model.IsEditMode ? "Update" : "Add")
                        </button>
                        @if (Model.IsEditMode)
                        {
                            <a asp-action="SubCategories" class="btn btn-secondary">
                                <i class='bx bx-x me-1'></i> Cancel
                            </a>
                        }
                    </div>
                </div>
            </div>

            <!-- Validation Message Row -->
            <div class="row">
                <div class="col-md-5">
                    <span asp-validation-for="CurrentSubCategory.Category_ID" class="text-danger small"></span>
                </div>
                <div class="col-md-7">
                    <span asp-validation-for="CurrentSubCategory.SubCategoryName" class="text-danger small"></span>
                </div>
            </div>
        </form>
    </div>
</div>

<!-- SubCategories List Card -->
<div class="card card-outline rounded-0 card-navy">
    <div class="card-header">
        <h5 class="card-title mb-0">
            <i class='bx bx-list-ul me-1'></i> Sub Category List
        </h5>
    </div>
    <div class="card-body p-0">
        @if (Model.SubCategoryList.Any())
        {
            <div class="table-responsive">
                <table class="table table-hover table-striped table-bordered dataTable">
                    <thead class="table-dark">
                        <tr>
                            <th>S.No</th>
                            <th>Sub Category Name</th>
                            <th>Category Name</th>
                            <th>Status</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @{
                            int index = 1;
                        }
                        @foreach (var item in Model.SubCategoryList)
                        {
                            <tr class="@(!item.IsActive ? "table-secondary" : "")">
                                <td>
                                    <span class="badge bg-light text-dark">@index</span>
                                </td>
                                <td>
                                        <strong>@item.SubCategoryName</strong>
                                </td>
                                <td>
                                    <span class="badge bg-label-primary">@item.Category?.CategoryName</span>
                                </td>
                                <td>
                                    @if (item.IsActive)
                                    {
                                        <span class="badge bg-success"><i class='bx bx-check-circle me-1'></i>Active</span>
                                    }
                                    else
                                    {
                                        <span class="badge bg-danger"><i class='bx bx-x-circle me-1'></i>Inactive</span>
                                    }
                                </td>
                                <td>
                                    <div class="d-flex gap-2">
                                        <a asp-action="EditSubCategory"
                                            asp-route-id="@Utility.EncryptURL(item.SubCategoryID.ToString2())"
                                            class="btn btn-sm btn-info text-white" title="Edit">
                                            <i class='bx bx-edit'></i>
                                        </a>
                                        <form asp-action="DeleteSubCategory"
                                            asp-route-id="@Utility.EncryptURL(item.SubCategoryID.ToString2())" method="post"
                                            class="d-inline">
                                            @Html.AntiForgeryToken()
                                            <button type="button"
                                                class="btn btn-sm @(item.IsActive ? "btn-warning" : "btn-success") delete-btn"
                                                title="@(item.IsActive ? "Deactivate" : "Activate")"
                                                data-name="@item.SubCategoryName" data-active="@item.IsActive">
                                                <i class='bx @(item.IsActive ? "bx-power-off" : "bx-check")'></i>
                                            </button>
                                        </form>
                                    </div>
                                </td>
                            </tr>
                            index++;
                        }
                    </tbody>
                </table>
            </div>
        }
        else
        {
            <div class="text-center py-5">
                <i class='bx bx-subdirectory-right display-1 text-muted'></i>
                <p class="text-muted mt-3">No sub categories found.</p>
            </div>
        }
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script>
        $(document).ready(function () {
            $('.delete-btn').click(function (e) {
                e.preventDefault();
                var form = $(this).closest('form');
                var name = $(this).data('name');
                var isActive = $(this).data('active') === 'True';
                var action = isActive ? "Deactivate" : "Activate";
                var btnColor = isActive ? "#d33" : "#28a745";

                Swal.fire({
                    title: `${action} Sub Category?`,
                    text: `Are you sure you want to ${action.toLowerCase()} "${name}"?`,
                    icon: 'warning',
                    showCancelButton: true,
                    confirmButtonColor: btnColor,
                    cancelButtonColor: "#6B7280",
                    confirmButtonText: `Yes, ${action}!`
                }).then(function (result) {
                    if (result.value) {
                        form.submit();
                    }
                });
            });

        @if (Model.IsEditMode)
        {
            <text>
                        $('#CurrentSubCategory_SubCategoryName').focus().select();
            </text>
        }
            });
    </script>
}
